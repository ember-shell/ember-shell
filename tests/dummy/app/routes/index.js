import Ember from 'ember';

export default Ember.Route.extend({

/*  manager: Ember.inject.service('shell-manager'),

  init(){
    this._super(...arguments);
    this.get('manager').addPanel({ isPrimary: true });
    this.get('manager').addWorkspace();
  }*/

});
