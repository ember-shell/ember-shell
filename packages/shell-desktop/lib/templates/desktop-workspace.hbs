{{#each apps as |app|}}
  {{#shell/desktop-window app=app as |window|}}
    {{window.titleBar app=app}}
    {{window.appContainer app=app}}
    {{#if app.hasStatusBar }}
      {{window.statusBar app=app}}
    {{/if}}
  {{/shell/desktop-window}}
{{/each}}