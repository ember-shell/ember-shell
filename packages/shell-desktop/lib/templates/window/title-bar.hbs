{{#unless yield}}
  {{#if showIcon}}
    {{shell/ui-icon iconUrl=app.icon iconSize="24"}}
  {{/if}}
  <div class="flex-box flex-center esh-window-title-bar-title esh-ui-draggable-handle">{{app.title}}</div>
{{else}}
  {{yield}}
{{/unless}}