{{#if hasBlock}}
  {{yield}}
{{else}}
  {{#if application.icon}}
    {{shell/ui-icon iconUrl=application.icon iconSize="32"}}
  {{/if}}
  <div class="flex-box flex-center esh-window-title-bar-title esh-ui-draggable-handle">{{application.title}}</div>
{{/if}}