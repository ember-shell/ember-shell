{{#unless yield}}

  {{#each manager.workspaces as |ws|}}
    {{shell/desktop-workspace workspace=ws}}
  {{/each}}

  {{#each panels.panels as |panel|}}
    {{shell/desktop-panel panel=panel}}
  {{/each}}

{{else}}
  {{yield}}
{{/unless}}